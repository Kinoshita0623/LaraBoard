<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title>{{ $topic->title }}</title>
    </head>
    <body>
        <h1>{{ $topic->title }}</h1>
        <p>{{ $topic->author }}</p>
        <p>{{ $topic->text }}</p>
    </body>
</html>
